import type { Metadata } from "next";import { AppShell } from "@/components/app-shell";import { weeks } from "@/lib/demo-data";
export const metadata:Metadata={title:"Season"};
export default function Page(){return <AppShell><main className="content"><div className="page-head"><div><p className="eyebrow">The full script</p><h1>Season</h1><p className="subtle">Competition weeks follow the calendar—not official matchweeks.</p></div></div>
<div className="card" style={{marginBottom:18}}><div className="stat-row" style={{marginTop:0}}><div><span className="stat-label" style={{color:"var(--muted)"}}>Active weeks</span><span className="stat-value">38</span></div><div><span className="stat-label" style={{color:"var(--muted)"}}>Completed</span><span className="stat-value">3</span></div><div><span className="stat-label" style={{color:"var(--muted)"}}>Next break</span><span className="stat-value">Oct 7</span></div></div></div>
<div className="timeline">{weeks.map((w,i)=><div className={`week ${w.state}`} key={i}><div className="week-icon">{w.state==="done"?"✓":w.n}</div><div className="week-body"><div className="week-title"><span>{w.title}</span><span className="subtle" style={{fontSize:12}}>{w.date}</span></div><div className="week-meta">{w.note}</div></div></div>)}</div>
<div className="notice" style={{marginTop:18}}><b>Second Half begins after Week 19.</b><br/>Bankroll carries forward, while the Second Half performance table starts from zero.</div></main></AppShell>}
